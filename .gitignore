# ---- 不要提交：厂商 / AOSP 专有二进制（分析用，发布前请删除）----
libwvhidl.so
android.hardware.drm@1.3-service.*
drmserver
libdrmframework*.so

# ---- 不要提交：设备崩溃现场（含 Build fingerprint）----
# docs/tombstones/

# ---- 构建产物 ----
drmdaemon-hook/src/main/cpp/*.so
release/
*.zip

# ---- 杂项 ----
.DS_Store
.idea/
